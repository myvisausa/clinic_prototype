{
  "name": "design-prototypes",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=web",
    "storybook": "npm run storybook --workspace=ui",
    "build-storybook": "npm run build-storybook --workspace=ui --"
  }
}
