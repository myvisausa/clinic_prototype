export function availability(tab, view, mode) {
  const base = `#page=schedule&tab=${encodeURIComponent(tab)}${view ? `&view=${view}` : ""}`;
  const weekly = mode !== "exceptions";
  return `<div class="schedule-overlay"><aside class="schedule-drawer" role="dialog" aria-label="Mi disponibilidad"><a class="schedule-drawer-close" aria-label="Cerrar" href="${base}">✕</a><h2>Mi disponibilidad</h2><p>Define tus horas semanales, excepciones del día y la conexión de tu calendario.</p><p class="schedule-drawer-hint">Elige si este empleado sigue el horario de la ubicación o usa una semana personalizada.</p><nav><a class="${weekly ? "active" : ""}" href="${base}&availability=weekly">Horas semanales</a><a class="${!weekly ? "active" : ""}" href="${base}&availability=exceptions">Excepciones del día</a></nav>${weekly ? `<div class="schedule-availability-body"><div class="schedule-toggle">Usar el horario de la ubicación <span>●</span></div><p>Este empleado sigue el horario de la ubicación.</p><hr><p>Los horarios se interpretan en la zona horaria de la ubicación · El botón de copiar aplica el horario de un día a los otros días seleccionados</p><button type="button" data-inert>Guardar</button></div>` : `<div class="schedule-availability-body"><p>Excepciones para los próximos 30 días (tiempo libre u horas personalizadas para una fecha).</p><label>Fecha <input type="date"></label><label>Tipo <select><option>Tiempo libre</option></select></label><input placeholder="Notas (opcional)"><button type="button" disabled>Agregar excepción</button><p>No hay excepciones en los próximos 30 días</p></div>`}<div class="schedule-google"><h3>Google Calendar</h3><p>Conectar Google Calendar permite que Avance acceda a listas de calendarios, eventos y disponibilidad para sincronizar citas.</p><p>Conecta tu Google Calendar</p><button type="button" disabled>Conectar Calendar</button><small>No se encontró un perfil de empleado. No se puede cambiar la sincronización del calendario.</small></div></aside></div>`;
}
